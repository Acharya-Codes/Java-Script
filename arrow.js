const sum = (a,b) => a+b; // If two parameters there then paranthesis is needed
const greet = () => {
    console.log("Hello"); // If emopty then also paranthesis needed
}
greet();
const sq = a => a*a; // If only one parameter no paranthesis needed